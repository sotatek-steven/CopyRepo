import DesignLayout from '@/components/layout/DesignLayout';
import React from 'react';

const FunctionPage = () => {
  return <div>FunctionPage</div>;
};

FunctionPage.PageLayout = (props) => DesignLayout({ children: props.children });

export default FunctionPage;
